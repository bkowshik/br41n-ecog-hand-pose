# 07. Deep Learning


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Imports

## EEGNet

Compact two-block CNN from Lawhern et al. (2018). Block 1 learns a
temporal filter then a per-electrode spatial filter; Block 2 is a
separable convolution. Tiny by deep-learning standards (~2k parameters),
which is what makes it fit 90 trials without overfitting
catastrophically.

------------------------------------------------------------------------

### EEGNet

``` python

def EEGNet(
    n_classes, n_channels, n_samples, F1:int=8, D:int=2, F2:int=16, kernel_length:int=64, dropout:float=0.5
):

```

*Base class for all neural network modules.*

Your models should also subclass this class.

Modules can also contain other Modules, allowing them to be nested in a
tree structure. You can assign the submodules as regular attributes::

    import torch.nn as nn
    import torch.nn.functional as F

    class Model(nn.Module):
        def __init__(self) -> None:
            super().__init__()
            self.conv1 = nn.Conv2d(1, 20, 5)
            self.conv2 = nn.Conv2d(20, 20, 5)

        def forward(self, x):
            x = F.relu(self.conv1(x))
            return F.relu(self.conv2(x))

Submodules assigned in this way will be registered, and will also have
their parameters converted when you call :meth:`to`, etc.

.. note:: As per the example above, an `__init__()` call to the parent
class must be made before assignment on the child.

:ivar training: Boolean represents whether this module is in training or
evaluation mode. :vartype training: bool

## Training loop

Per-fold training: Adam, cross-entropy, fixed epoch count. Returns the
trained model so the caller can run inference on a held-out fold.

------------------------------------------------------------------------

### train_eegnet

``` python

def train_eegnet(
    epochs, y, n_epochs:int=200, batch_size:int=16, lr:float=0.001, device:str='cpu', net_kwargs:VAR_KEYWORD
):

```

*Train an EEGNet on `(trials, channels, samples)` epochs with integer
labels `y`.*

## Cross-validation

Stratified k-fold mirroring `06_classification.cross_validate`. Labels
are remapped to `0..n_classes-1` per fold for the cross-entropy loss.

------------------------------------------------------------------------

### cross_validate_eegnet

``` python

def cross_validate_eegnet(
    epochs, classes, n_splits:int=5, random_state:int=0, device:str='cpu', train_kwargs:VAR_KEYWORD
):

```

*Stratified k-fold accuracy for EEGNet on `(trials, channels, samples)`
epochs.*

## Run

``` python
from br41n_ecog_hand_pose.data import load_ecog
from br41n_ecog_hand_pose.preprocessing import preprocess
from br41n_ecog_hand_pose.epoching import epoch_recording

rec = load_ecog()
clean, _ = preprocess(rec.ecog, rec.fs)
epochs, classes = epoch_recording(rec, tmin=0.0, tmax=2.0, signal=clean)
print(f'epochs {epochs.shape}, classes {classes.shape}')
```

``` python
accs = cross_validate_eegnet(epochs.astype('float32'), classes, n_splits=5, n_epochs=300, lr=1e-3)
print(f'EEGNet 5-fold accuracy: {accs.mean():.3f} \u00b1 {accs.std():.3f}')
```
