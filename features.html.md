# 05. Feature Extraction


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Frequency bands

Standard motor-decoding bands. High-gamma is the workhorse for ECoG hand
decoding, but the mu/beta event-related desynchronization (ERD) is
informative too.

## Single-band power

Average PSD power between `low` and `high` Hz. Returns
`(trials, channels)` when given an epoched tensor, or `(channels,)` when
given continuous data.

------------------------------------------------------------------------

### band_power

``` python

def band_power(
    x, fs, low, high, nperseg:NoneType=None
):

```

*Mean PSD power in `[low, high]` Hz along the last axis of `x`.*

## Multi-band feature matrix

Stack band power across `bands` into a `(trials, channels × n_bands)`
matrix ready for a classifier. Log-transform is on by default because
band power is approximately log-normal — the transformed features fit
Gaussian classifiers (LDA, QDA) much better.

------------------------------------------------------------------------

### multi_band_power

``` python

def multi_band_power(
    epochs, fs, bands:NoneType=None, log:bool=True
):

```

*Concatenate band-power features over `bands` along the last axis.*

## Z-score normalization

Center each feature and scale to unit variance. Fit on the training
fold; apply to the test fold. Avoids per-channel amplitude differences
dominating the classifier.

------------------------------------------------------------------------

### zscore_apply

``` python

def zscore_apply(
    X, stats
):

```

*Call self as a function.*

------------------------------------------------------------------------

### zscore_fit

``` python

def zscore_fit(
    X
):

```

*Return `(mean, std)` along axis 0 for later application via
`zscore_apply`.*

## Visual validation

Plot mean log-band-power per class for the high-gamma band. If the
classes look separable here, a linear classifier should do well in `06`.

``` python
import matplotlib.pyplot as plt
from br41n_ecog_hand_pose.data import load_ecog, GESTURE_NAMES
from br41n_ecog_hand_pose.preprocessing import preprocess
from br41n_ecog_hand_pose.epoching import epoch_recording

plt.rcParams.update({
    'axes.grid':      True,
    'grid.linestyle': ':',
    'grid.linewidth': 0.5,
    'grid.alpha':     0.6,
})
```

``` python
rec = load_ecog()
clean, _ = preprocess(rec.ecog, rec.fs)
epochs, classes = epoch_recording(rec, tmin=0.0, tmax=2.0, signal=clean)
X = multi_band_power(epochs, rec.fs)
print(f'epochs:   {epochs.shape}')
print(f'features: {X.shape}     # 60 channels \u00d7 {len(__import__("br41n_ecog_hand_pose.features", fromlist=["BANDS"]).BANDS)} bands')
```

``` python
# split features back into (trials, channels, bands) for plotting
n_ch = epochs.shape[1]
F = X.reshape(X.shape[0], len(BANDS), n_ch).transpose(0, 2, 1)  # (trials, channels, bands)

fig, axs = plt.subplots(1, len(BANDS), figsize=(4 * len(BANDS), 3), sharey=True)
for ax, (band_name, _), bi in zip(axs, BANDS.items(), range(len(BANDS))):
    for c in [1, 2, 3]:
        ax.plot(F[classes == c, :, bi].mean(axis=0), label=GESTURE_NAMES[c])
    ax.set_title(band_name)
    ax.set_xlabel('channel')
axs[0].set_ylabel('mean log-power')
axs[0].legend(fontsize=8)
fig.suptitle('Mean log-band-power per channel, per class')
plt.tight_layout(); plt.show()
```
